import React from "react";
import Navbar from "./navbar";
import Login from "../login/login";

const Layout = ({}) => {
    return (
        <div>
            <Navbar></Navbar>
            <Login></Login>
        </div>
    );
};
export default Layout;
